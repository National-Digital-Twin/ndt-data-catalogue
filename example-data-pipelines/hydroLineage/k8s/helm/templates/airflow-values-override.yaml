# SPDX-License-Identifier: Apache-2.0
#
# © Crown Copyright 2026. This work has been developed by the National Digital Twin
# Programme and is legally attributed to the UK's Department for Business, Innovation, Science and Trade (BIST) as the governing
# entity

{{- /*
This template overrides specific Airflow values with dynamic release name
*/ -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-airflow-config
  namespace: {{ .Release.Namespace }}
data:
  warehouse-host: "{{ .Release.Name }}-warehouse-postgresql"
  metadata-host: "{{ .Release.Name }}-postgresql"
  dags-configmap: "{{ .Release.Name }}-dags"
